
<ul>
  <% @@cookbook.all.each_with_index do |x, ind| %>
    <li>
      <%= ind + 1 %>. <%= x.tried ? "<span class='glyphicon glyphicon-ok'></span>" : "<span class='glyphicon glyphicon-remove'></span>" %> <%=x.name%>. Prep and cooking time: <%=x.time%> <br>
      <%= x.description %>
    </li>
  <% end %>
</ul>

<p><a href="/">Home</a></p>
